(()=>{
const style=document.createElement('style');style.textContent=`.neko-runner-extra{margin-top:10px;padding-top:10px;border-top:1px solid #263247;display:grid;gap:8px}.neko-workload{background:#0b1220;border:1px solid #23324a;border-radius:9px;padding:9px}.neko-workload-label{font-size:9px;text-transform:uppercase;letter-spacing:.08em;color:#7f91aa;margin-bottom:4px}.neko-workload-repo{font-size:13px;font-weight:800;color:#9fb7ff}.neko-workload-title{font-size:12px;font-weight:700;margin-top:2px}.neko-workload-meta,.neko-runner-node,.neko-control-state,.neko-action-msg{font-size:10px;color:#8fa0b8;margin-top:3px}.neko-control-row{display:flex;gap:6px;align-items:center;flex-wrap:wrap}.neko-control-btn{border:1px solid #33425a;background:#172033;color:#e8edf7;border-radius:7px;padding:6px 9px;font:600 10px system-ui;cursor:pointer}.neko-control-btn.stop{color:#ffabb4;border-color:#633b45;background:#25151b}.neko-control-btn.start{color:#77e5ae;border-color:#315b49;background:#10231c}.neko-control-btn.restart{color:#ffd879;border-color:#64552b;background:#241f10}.neko-control-state.stopped{color:#ff8793}.neko-repo-cell b{display:inline-block;background:#16243c;border:1px solid #2d4267;border-radius:6px;padding:4px 7px;color:#a9c3ff}`;document.head.appendChild(style);
let data={overview:null,nodes:null,controls:null},loading=false;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function api(url,opt={}){const r=await fetch(url,{credentials:'same-origin',cache:'no-store',...opt,headers:{...(opt.body?{'content-type':'application/json'}:{}),...(opt.headers||{})}});if(r.status===401){location='/login';throw Error('Authentication required')}if(!r.ok){let e;try{e=(await r.json()).error}catch{e=await r.text()}throw Error(e||`HTTP ${r.status}`)}return r.json()}
function stateMaps(){const runs=new Map((data.overview?.runs||[]).map(r=>[String(r.id),r])),work=new Map();for(const j of data.overview?.active_jobs||[]){if(!j.runner_name)continue;const r=runs.get(String(j.run_id));work.set(j.runner_name,{repo:j.repo,workflow:r?.display_title||r?.name||'Workflow',branch:r?.branch||'–',job:j.name,status:j.status})}return{work,nodesByRunner:new Map((data.nodes?.nodes||[]).filter(n=>n.runner_name).map(n=>[n.runner_name,n])),nodesById:new Map((data.nodes?.nodes||[]).map(n=>[n.id,n])),controls:new Map((data.controls?.controls||[]).map(c=>[c.runner_name,c]))}}
function workload(w){return w?`<div class="neko-workload"><div class="neko-workload-label">Current repository / workload</div><div class="neko-workload-repo">${esc(w.repo)}</div><div class="neko-workload-title">${esc(w.workflow)}</div><div class="neko-workload-meta">branch ${esc(w.branch)} • job ${esc(w.job)} • ${esc(w.status)}</div></div>`:`<div class="neko-workload"><div class="neko-workload-label">Current repository / workload</div><div class="neko-workload-title">Idle — no repository assigned</div></div>`}
function controls(node,runner,c){if(!node)return'<div class="neko-control-state">No matching node agent — controls unavailable.</div>';const stopped=c?.desired_state==='stopped';return`<div class="neko-control-row"><button class="neko-control-btn ${stopped?'start':'stop'}" data-rc-action="${stopped?'start':'stop'}" data-rc-node="${esc(node.id)}" data-rc-runner="${esc(runner)}">${stopped?'▶ Start runner':'■ Stop runner'}</button><button class="neko-control-btn restart" data-rc-action="restart" data-rc-node="${esc(node.id)}" data-rc-runner="${esc(runner)}">↻ Restart</button><span class="neko-control-state ${stopped?'stopped':''}">${stopped?'Stopped by dashboard':'Managed / running'}</span></div><div class="neko-action-msg" data-rc-msg="${esc(node.id)}"></div>`}
function render(){if(!data.overview)return;const m=stateMaps();document.querySelectorAll('#runnerList .runner').forEach(card=>{const name=card.querySelector('.runner-name')?.textContent?.trim();if(!name)return;let box=card.querySelector('.neko-runner-extra');if(!box){box=document.createElement('div');box.className='neko-runner-extra';card.appendChild(box)}const html=workload(m.work.get(name))+controls(m.nodesByRunner.get(name),name,m.controls.get(name));if(box.dataset.html!==html){box.dataset.html=html;box.innerHTML=html}});document.querySelectorAll('.node-card[data-node]').forEach(card=>{const n=m.nodesById.get(card.dataset.node);if(!n)return;let box=card.querySelector('.neko-runner-extra');if(!box){box=document.createElement('div');box.className='neko-runner-extra';card.appendChild(box)}const html=workload(m.work.get(n.runner_name))+controls(n,n.runner_name,m.controls.get(n.runner_name));if(box.dataset.html!==html){box.dataset.html=html;box.innerHTML=html}});document.querySelectorAll('#workflowRows tr').forEach(row=>{const cells=row.querySelectorAll('td');if(!cells.length)return;cells[0].classList.add('neko-repo-cell');const runner=(cells[4]?.firstChild?.textContent||cells[4]?.textContent||'').trim();if(runner&&runner!=='–'&&!cells[4].querySelector('.neko-runner-node')){const n=m.nodesByRunner.get(runner);if(n){const d=document.createElement('div');d.className='neko-runner-node';d.textContent=`Node: ${n.name}`;cells[4].appendChild(d)}}});bind()}
function bind(){document.querySelectorAll('[data-rc-action]').forEach(b=>{if(b.dataset.rcBound)return;b.dataset.rcBound='1';b.onclick=async e=>{e.preventDefault();e.stopPropagation();const action=b.dataset.rcAction,node=b.dataset.rcNode,runner=b.dataset.rcRunner,w=stateMaps().work.get(runner);if(action==='stop'&&!confirm(w?`Stop ${runner}?\n\nActive repo: ${w.repo}\nWorkflow: ${w.workflow}\n\nThis can interrupt or fail the current GitHub job.`:`Stop ${runner}?`))return;if(action==='restart'&&w&&!confirm(`Restart ${runner}?\n\nActive repo: ${w.repo}\nWorkflow: ${w.workflow}\n\nThis can interrupt the current GitHub job.`))return;b.disabled=true;const msg=document.querySelector(`[data-rc-msg="${CSS.escape(node)}"]`);if(msg)msg.textContent=`Sending ${action}…`;try{await api('/api/node/runner-control',{method:'POST',body:JSON.stringify({node_id:node,action})});if(msg)msg.textContent=`${action} accepted`;setTimeout(refresh,600)}catch(err){if(msg)msg.textContent=`Error: ${err.message}`}finally{b.disabled=false}}})}
async function refresh(){if(loading)return;loading=true;try{const[o,n,c]=await Promise.all([api('/api/overview'),api('/api/nodes'),api('/api/runner-controls')]);data={overview:o,nodes:n,controls:c};render()}catch(e){console.error('[runner-controls]',e)}finally{loading=false}}
setInterval(render,2000);setInterval(refresh,10000);setTimeout(refresh,500);
})();
