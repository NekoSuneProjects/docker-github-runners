services:
  github-builder:
    image: ${RUNNER_IMAGE:-neko-github-builder:latest}
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      RUNNER_SCOPE: ${RUNNER_SCOPE:-organization}
      GITHUB_ORG: ${GITHUB_ORG}
      REPO_URL: ${REPO_URL:-}
      ACCESS_TOKEN: ${ACCESS_TOKEN}
      RUNNER_NAME: ${RUNNER_NAME:-neko-builder}
      LABELS: ${LABELS:-docker,buildx,multiarch,builder}
      RUNNER_UPDATE_ON_START: ${RUNNER_UPDATE_ON_START:-true}
      ENABLE_MULTIARCH_ON_START: ${ENABLE_MULTIARCH_ON_START:-true}
      MULTIARCH_PLATFORMS: ${MULTIARCH_PLATFORMS:-arm64,amd64}
    labels:
      neko.runner.managed: "true"
      neko.runner.name: "${RUNNER_NAME:-neko-builder}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - runner-work:/work
      - runner-diag:/actions-runner/_diag
    init: true
    stop_grace_period: 30s
    restart: unless-stopped

  dashboard:
    image: ${DASHBOARD_IMAGE:-neko-github-runner-dashboard:latest}
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    environment:
      GITHUB_ORG: ${GITHUB_ORG}
      ACCESS_TOKEN: ${ACCESS_TOKEN}
      GITHUB_DASHBOARD_TOKEN: ${GITHUB_DASHBOARD_TOKEN:-}
      DASHBOARD_REPOS: ${DASHBOARD_REPOS:-}
      DASHBOARD_MAX_REPOS: ${DASHBOARD_MAX_REPOS:-12}

      # GitHub API data is intentionally slower than node heartbeats. The
      # dashboard shares/caches GitHub responses across all tabs and clients.
      DASHBOARD_REFRESH_SECONDS: ${DASHBOARD_REFRESH_SECONDS:-30}
      DASHBOARD_LIVE_GITHUB_SECONDS: ${DASHBOARD_LIVE_GITHUB_SECONDS:-30}
      DASHBOARD_SSE_KEEPALIVE_SECONDS: ${DASHBOARD_SSE_KEEPALIVE_SECONDS:-15}
      DASHBOARD_GITHUB_CACHE_SECONDS: ${DASHBOARD_GITHUB_CACHE_SECONDS:-30}
      DASHBOARD_GITHUB_RUNS_CACHE_SECONDS: ${DASHBOARD_GITHUB_RUNS_CACHE_SECONDS:-60}
      DASHBOARD_GITHUB_JOBS_CACHE_SECONDS: ${DASHBOARD_GITHUB_JOBS_CACHE_SECONDS:-30}
      DASHBOARD_GITHUB_RUNNERS_CACHE_SECONDS: ${DASHBOARD_GITHUB_RUNNERS_CACHE_SECONDS:-30}
      DASHBOARD_GITHUB_REPOS_CACHE_SECONDS: ${DASHBOARD_GITHUB_REPOS_CACHE_SECONDS:-300}
      DASHBOARD_GITHUB_STALE_SECONDS: ${DASHBOARD_GITHUB_STALE_SECONDS:-900}
      DASHBOARD_GITHUB_RATE_RESERVE: ${DASHBOARD_GITHUB_RATE_RESERVE:-500}

      # All organization runners are paginated into SQLite. Old registrations
      # remain stored/offline instead of disappearing on partial API responses.
      DASHBOARD_GITHUB_RUNNER_SYNC_SECONDS: ${DASHBOARD_GITHUB_RUNNER_SYNC_SECONDS:-60}
      DASHBOARD_GITHUB_RUNNER_MAX_PAGES: ${DASHBOARD_GITHUB_RUNNER_MAX_PAGES:-100}

      DASHBOARD_AUTH_REQUIRED: ${DASHBOARD_AUTH_REQUIRED:-true}
      DASHBOARD_USERNAME: ${DASHBOARD_USERNAME:-}
      DASHBOARD_PASSWORD: ${DASHBOARD_PASSWORD:-}
      DASHBOARD_PASSWORD_SHA256: ${DASHBOARD_PASSWORD_SHA256:-}
      DASHBOARD_SESSION_SECRET: ${DASHBOARD_SESSION_SECRET:-}
      DASHBOARD_SESSION_TTL_HOURS: ${DASHBOARD_SESSION_TTL_HOURS:-12}
      DASHBOARD_COOKIE_SECURE: ${DASHBOARD_COOKIE_SECURE:-false}

      DASHBOARD_NODE_SHARED_SECRET: ${DASHBOARD_NODE_SHARED_SECRET:-}
      DASHBOARD_NODE_OFFLINE_SECONDS: ${DASHBOARD_NODE_OFFLINE_SECONDS:-45}
      DASHBOARD_NODE_MAX_LOG_BYTES: ${DASHBOARD_NODE_MAX_LOG_BYTES:-262144}
      DASHBOARD_NODE_MAX_BODY_BYTES: ${DASHBOARD_NODE_MAX_BODY_BYTES:-1048576}

      DASHBOARD_DB_FILE: /data/dashboard.sqlite
      DASHBOARD_DATA_DIR: /data
      DASHBOARD_LOG_RETENTION_DAYS: ${DASHBOARD_LOG_RETENTION_DAYS:-30}
      DASHBOARD_LOG_MAX_ROWS_PER_NODE: ${DASHBOARD_LOG_MAX_ROWS_PER_NODE:-500}
      RUNNER_DIAG_DIR: /runner-diag

    ports:
      - "${DASHBOARD_BIND:-127.0.0.1}:${DASHBOARD_PORT:-8080}:8080"
    volumes:
      - runner-diag:/runner-diag:ro
      - ./data:/data
    depends_on:
      - github-builder
    init: true
    stop_grace_period: 10s
    restart: unless-stopped

  node-agent:
    image: ${NODE_AGENT_IMAGE:-neko-github-runner-node-agent:latest}
    build:
      context: ./node-agent
      dockerfile: Dockerfile
    environment:
      DASHBOARD_URL: http://dashboard:8080
      DASHBOARD_NODE_SHARED_SECRET: ${DASHBOARD_NODE_SHARED_SECRET:-}
      NODE_ID: ${NODE_ID:-central-node}
      NODE_NAME: ${NODE_NAME:-Central GitHub Builder}
      NODE_LOCATION: ${NODE_LOCATION:-}
      RUNNER_NAME: ${RUNNER_NAME:-neko-builder}
      RUNNER_SCOPE: ${RUNNER_SCOPE:-organization}
      GITHUB_ORG: ${GITHUB_ORG}
      REPO_URL: ${REPO_URL:-}
      ACCESS_TOKEN: ${ACCESS_TOKEN}
      NODE_LABELS: ${LABELS:-docker,buildx,multiarch,builder}
      NODE_HEARTBEAT_SECONDS: ${NODE_HEARTBEAT_SECONDS:-10}
      NODE_LOG_TAIL_BYTES: ${NODE_LOG_TAIL_BYTES:-131072}
      RUNNER_DIAG_DIR: /runner-diag
      NODE_HOST_ROOT: /host

      NODE_STUCK_WATCHDOG_ENABLED: ${NODE_STUCK_WATCHDOG_ENABLED:-true}
      NODE_STUCK_THRESHOLD_SECONDS: ${NODE_STUCK_THRESHOLD_SECONDS:-600}
      NODE_AUTO_RECOVER_STUCK: ${NODE_AUTO_RECOVER_STUCK:-false}
      NODE_STUCK_RECOVERY_COOLDOWN_SECONDS: ${NODE_STUCK_RECOVERY_COOLDOWN_SECONDS:-900}

      NODE_AUTO_RECOVER_OFFLINE: ${NODE_AUTO_RECOVER_OFFLINE:-true}
      NODE_RUNNER_HEALTHCHECK_SECONDS: ${NODE_RUNNER_HEALTHCHECK_SECONDS:-30}
      NODE_RUNNER_OFFLINE_RECOVERY_SECONDS: ${NODE_RUNNER_OFFLINE_RECOVERY_SECONDS:-120}
      NODE_RUNNER_OFFLINE_RECOVERY_COOLDOWN_SECONDS: ${NODE_RUNNER_OFFLINE_RECOVERY_COOLDOWN_SECONDS:-600}
      NODE_RUNNER_CONTAINER_LABEL: neko.runner.managed=true

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - runner-diag:/runner-diag
      - /etc/hostname:/host/etc/hostname:ro
      - /etc/os-release:/host/etc/os-release:ro
      - /proc:/host/proc:ro
    depends_on:
      - github-builder
      - dashboard
    init: true
    stop_grace_period: 5s
    restart: unless-stopped

volumes:
  runner-work:
  runner-diag:
